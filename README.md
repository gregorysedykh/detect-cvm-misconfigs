# Detecting Misconfigurations in Confidential Virtual Machine Deployments

**Gregory Igor Sedykh**

## Structure

This repository contains two main directories:
- `cvm-configs`: contains the configuration files for the Confidential Virtual Machine (CVM) deployments, including the misconfiguration scenarios.
- `custom_checks`: contains custom Checkov policies written in Python to detect misconfigurations in CVM deployments.